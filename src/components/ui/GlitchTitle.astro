---
interface Props {
  text: string;
  as?: 'h1' | 'h2' | 'h3';
  class?: string;
}

const { text, as: Tag = 'h1', class: className } = Astro.props;
---

<Tag class:list={['encodere-glitch','encodere-title', className]} data-text={text}>
  <slot>{text}</slot>
</Tag>
